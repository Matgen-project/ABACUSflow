1. 获得类似与DFT数据库中SP集合中的数据：
# -t: sp 为structure property, -s 指定SCF步骤中计算目录下的Abacus的结构格式文件
>>> python main.py -t sp -s ./test/icsd_9852-Ti2O4/SPIN1/icsd_9852-Ti2O4
# 返回值同dft_data数据库集合sp
2. 获取能带数据
>>> python main.py -t band -d ./test/icsd_9852-Ti2O4/SPIN1/ -l ./test/icsd_9852-Ti2O4/SPIN1/OUT.ABACUS/running_scf.log -s icsd_9852-Ti2O4
# -t: band, 同上； -d 指定计算能带的目录；由于Abacus在能带计算式不会输出费米能级等信息，
# 因此需要指定前一步的running_scf.log的文件路径（注意：在计算能带是需要将上一步SCF中的SPIN1_CHGCAR
# 和SPIN2_CHGCAR（如果存在）拷入能带计算文件夹下再提交计算）， -s 无需指定结构文件的路径，指定当前能带
# 目录下的结构文件名即可
# 返回值同dft_data数据库集合electronic_property
3. 获取dos数据
>>> python main.py -t dos -d ./test/icsd_23076-Sr1Ti1O3/DOS_SPIN2
# -t 同理, -d 指定计算dos的目录
# 返回值同dft_data数据库集合electronic_property
4. 获取磁性性质
>>> python main.py -t mag -d ./test/icsd_23076-Sr1Ti1O3/SCF
# 注: 由于ABACUS目前只能输出整体的磁矩，因此返回为{"total": xxx(float)}
5. 获取优化后的能量信息
>>> python main.py -t energy -d ./test/icsd_23076-Sr1Ti1O3/SCF
# 返回值为 {'final': xxx(float)}
6. 获取费米能级
>>> python main.py -t efermi -d ./test/icsd_23076-Sr1Ti1O3/SCF/
# 返回值为 {'efermi': xxx(float)}
7. 获取优化后的cif内容
>>> python main.py -t cif -s ./test/icsd_23076-Sr1Ti1O3/SCF/icsd_23076-Sr1Ti1O3
# 返回值同dft_data数据库中集合optimized_structure_cif{'cif_data': xxx(str)}
8. 获取未优化的poscar内容
>>> python main.py -t poscar -s ./test/icsd_23076-Sr1Ti1O3/CELL_RELAX/icsd_23076-Sr1Ti1O3
# 注: 由于是未优化的信息，需要指定relax计算路径下的abacus初始结构文件路径
# 返回值同dft_data数据库中集合unoptimized_poscar
9. 生成能带计算中的KPT
>>> python main.py -t kpath -s ./test/icsd_23076-Sr1Ti1O3/CELL_RELAX/icsd_23076-Sr1Ti1O3 -n 20
# 注: 用于生成能带计算中的KPT文件，因为AbacusHT脚本并未提供此选项，-s 指定结构文件,
# -n 指定对称点之间的点数， 默认为20
10. 获取优化后的最终结构文件路径
>>> python main.py -t opt -d ./test/icsd_23076-Sr1Ti1O3/CELL_RELAX/
# -d 指定Relax的目录，由于Abacus是将每一步优化的结构写成新的STRU_ION?_D文件，
# 因此需要再优化后将最终的STRU_ION?_D文件拷贝到后续的计算中作为新的Stru文件
# 返回最后的结构文件路径：如： ./test/icsd_23076-Sr1Ti1O3/CELL_RELAX/OUT.ABACUS\STRU_ION5_D
11. 获取KPT的内容
>>> python main.py -t kpts -d  ./test/icsd_23076-Sr1Ti1O3/CELL_RELAX/ -k relax
# 获取不同步骤的KPT文件内容, -k 指定计算的类型（relax,scf, band）；
# 返回值同dft_data数据库集合kpoints_data

注：以上返回值均未添加{"xxxx_id": xxxx}
